<?xml version="1.0" encoding="utf-8"?>
<!--
    Development network security policy.
    Permits cleartext HTTP/WS to localhost, emulator host, and RFC1918 LAN addresses
    so the agent can reach a backend served on the same network without TLS.

    For production deployments, replace this with a tighter config that lists only the
    actual backend domain over HTTPS.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
        <domain includeSubdomains="true">10.0.2.2</domain>
    </domain-config>
</network-security-config>
