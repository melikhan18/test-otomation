server:
  port: 8082
  forward-headers-strategy: framework

spring:
  application:
    name: android-device-service
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:qa_platform}
    username: ${DB_USER:dp}
    password: ${DB_PASSWORD:dp_dev_password}
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        default_schema: android_device
        jdbc.time_zone: UTC
  flyway:
    enabled: true
    default-schema: android_device
    schemas: android_device
    locations: classpath:db/migration
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}

app:
  jwt:
    secret: ${JWT_SECRET:Y2hhbmdlLW1lLWluLXByb2QtY2hhbmdlLW1lLWluLXByb2QtY2hhbmdlLW1lLWluLXByb2QtY2hhbmdlLW1lLWluLXByb2QtMTIzNDU2Nzg=}
    issuer: qa-platform
  device:
    heartbeat-ttl-seconds: 20

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics

logging:
  level:
    com.qaplatform: DEBUG
